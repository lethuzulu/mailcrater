# MailCrater

Self-hosted, accept-all SMTP server for local development. Catches outgoing
mail from any app pointed at it, stores it persistently, and lets you
inspect it via a web UI and JSON API. No caught message is ever delivered
anywhere.

<!-- TODO: quickstart, screenshot, usage -->
